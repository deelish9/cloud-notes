services:
  api:
    build: .
    container_name: notes_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/code
    dns:
      - 8.8.8.8
      - 1.1.1.1
    extra_hosts:
      - "host.docker.internal:host-gateway"
